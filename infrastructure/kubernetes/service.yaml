apiVersion: v1
kind: Service
metadata:
  name: ltmc-mcp-service
  labels:
    app: ltmc-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: ltmc-mcp-server
