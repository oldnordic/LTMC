# LTMC Configuration Example
# Copy this file to ltmc_config.env and modify as needed

# Database Configuration
DB_PATH=ltmc.db
FAISS_INDEX_PATH=faiss_index

# Logging Configuration
LOG_LEVEL=INFO

# HTTP Server Configuration
HTTP_HOST=localhost
HTTP_PORT=5050

# Optional: Neo4j Configuration (if using graph features)
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=password

# Optional: Redis Configuration (if using caching)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0

# Optional: Qdrant Configuration (if using vector search)
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
